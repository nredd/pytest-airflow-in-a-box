Render the `--airflow-smoke` parse and serialization report tables with an owned plain-text renderer instead of Airflow's unstable `airflow.cli.simple_table.AirflowConsole` CLI internal. Report content, sort order, and log lines are unchanged; long paths are no longer wrapped at 100 columns.
