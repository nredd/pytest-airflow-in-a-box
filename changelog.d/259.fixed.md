Refresh created task instances through the `dag_run.task_instances` relationship in `create_dagrun`, so the returned DagRun keeps the refreshed objects alive. Previously the refresh targeted a transient query result the session's weak identity map dropped at the next garbage collection, and any later lookup rehydrated fresh rows with `ti.task = None`.
