BREAKING: `check_component` now requires a custom executor to override `BaseExecutor.end`
and `BaseExecutor.terminate`, in addition to `sync` and `_process_workloads`. Both default
to raising `NotImplementedError`, and an executor missing either previously passed
`executor-missing-override` clean, only to fail at teardown or SIGTERM.
