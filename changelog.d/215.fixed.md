Fix the custom-components guide's executor alias documentation: `airflow_config(executor=...)` is
not a real signature -- the call is `airflow_config({("core", "executor"): alias})` -- and the
`airflow_executor` ini can never name a runtime alias, since it is resolved before the first
Airflow import, when no alias exists yet.
