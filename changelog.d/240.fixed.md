`clear_db`/`TableGroup.RUNS` and `dag_maker` fixture teardown now clear `Backfill`/
`BackfillDagRun` rows before deleting the `DagRun` rows they FK-reference, instead of
raising `sqlite3.IntegrityError: FOREIGN KEY constraint failed` (Airflow 3.x only, where
backfill tables exist).
