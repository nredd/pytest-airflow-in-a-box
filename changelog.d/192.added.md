`run_task` and `render_task` now accept an operator that is not bound to any Dag: the fixture auto-creates and binds a synthetic `airflow.sdk.DAG` in place, named by an explicit `dag_id=` or a deterministic per-test, xdist-safe identifier, removing the `dag=DAG(...)` boilerplate DB-free tests previously required.
